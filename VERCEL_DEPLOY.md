# 🚀 Развертывание на Vercel (Полный проект)

## Обзор

Этот проект настроен для полного развертывания на Vercel:
- **Frontend**: React + Vite SPA
- **Backend**: Express API через Vercel Serverless Functions

Все работает на одном домене без необходимости отдельного backend сервера!

---

## 📋 Быстрый старт

### 1️⃣ Подготовка проекта

Убедитесь, что все изменения закоммичены:

```bash
git add .
git commit -m "Prepare for Vercel deployment"
git push
```

### 2️⃣ Создание проекта на Vercel

1. Откройте [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New..."** → **"Project"**
4. Выберите ваш репозиторий
5. Нажмите **"Import"**

### 3️⃣ Настройка проекта

Vercel автоматически определит настройки из `vercel.json`, но проверьте:

**Build & Development Settings:**
- **Framework Preset**: Vite
- **Build Command**: `pnpm run build` (или оставьте пустым)
- **Output Directory**: `dist/client` (или оставьте пустым)
- **Install Command**: `pnpm install` (или оставьте пустым)

### 4️⃣ Переменные окружения

Добавьте необходимые переменные в **Settings → Environment Variables**:

#### Обязательные:
```env
NODE_ENV=production
```

#### Опциональные (если используются):
```env
VITE_PUBLIC_BUILDER_KEY=your_builder_io_key
GITHUB_TOKEN=your_github_token
PING_MESSAGE=Hello from Vercel!
FRONTEND_URL=https://your-app.vercel.app
```

**Важно:** Переменные с префиксом `VITE_` доступны в frontend коде.

### 5️⃣ Деплой

Нажмите **"Deploy"** и дождитесь завершения сборки.

---

## 🔍 Проверка работы

После успешного деплоя проверьте:

### Frontend:
```
https://your-app.vercel.app
```

### API Endpoints:
```
https://your-app.vercel.app/health
https://your-app.vercel.app/api/ping
https://your-app.vercel.app/api/demo
```

### CMS Admin:
```
https://your-app.vercel.app/admin
```

---

## 📁 Структура проекта

```
├── api/
│   └── index.ts              # Vercel Serverless Function (все API)
├── server/
│   ├── index.ts              # Express app (используется в api/index.ts)
│   └── routes/               # API маршруты
├── client/                   # React frontend
├── dist/client/              # Собранный frontend (после build)
├── vercel.json               # Конфигурация Vercel
└── package.json
```

---

## 🔄 Автоматические деплои

Vercel автоматически деплоит:
- **Production**: при push в `main` ветку
- **Preview**: при создании Pull Request

---

## ⚙️ Как это работает

### Frontend (SPA)
- Vite собирает React приложение в `dist/client/`
- Vercel отдает статические файлы
- React Router обрабатывает клиентскую маршрутизацию

### Backend (API)
- Все API запросы (`/api/*`) направляются в `api/index.ts`
- Express app обрабатывает запросы как Serverless Function
- Каждый запрос выполняется в изолированной среде

### Маршрутизация
```
/                    → React SPA (index.html)
/about               → React SPA (index.html)
/api/ping            → Serverless Function (api/index.ts)
/api/demo            → Serverless Function (api/index.ts)
/health              → Serverless Function (api/index.ts)
/admin               → React SPA (Decap CMS)
```

---

## 🛠️ Локальная разработка

### Запуск frontend + backend вместе:
```bash
pnpm dev
```
- Frontend: `http://localhost:8080`
- Backend API: `http://localhost:8080/api/*`

### Запуск только backend:
```bash
pnpm dev:server
```
- Backend: `http://localhost:3001`

### Тестирование Vercel локально:
```bash
# Установите Vercel CLI
npm i -g vercel

# Запустите локально
vercel dev
```

---

## 📊 Мониторинг и логи

### Просмотр логов:
1. Откройте проект в Vercel Dashboard
2. Перейдите в **Deployments**
3. Выберите деплой
4. Откройте вкладку **Functions** для логов API

### Метрики:
- **Analytics**: трафик и производительность
- **Speed Insights**: Core Web Vitals
- **Logs**: ошибки и запросы

---

## 🔧 Troubleshooting

### ❌ API возвращает 404
**Проблема:** Маршруты API не работают

**Решение:**
1. Проверьте `vercel.json` - должен быть rewrite для `/api/:path*`
2. Убедитесь, что `api/index.ts` экспортирует функцию по умолчанию
3. Проверьте логи в Vercel Dashboard

### ❌ CORS ошибки
**Проблема:** Frontend не может обращаться к API

**Решение:**
1. Убедитесь, что frontend и API на одном домене
2. Проверьте настройки CORS в `api/index.ts`
3. Добавьте `FRONTEND_URL` в переменные окружения

### ❌ Переменные окружения не работают
**Проблема:** `process.env.VARIABLE` возвращает undefined

**Решение:**
1. Переменные для frontend должны начинаться с `VITE_`
2. Переменные для API добавляются в Vercel Settings
3. После добавления переменных нужен redeploy

### ❌ Build fails
**Проблема:** Ошибка при сборке

**Решение:**
1. Проверьте логи сборки в Vercel
2. Убедитесь, что `pnpm run build` работает локально
3. Проверьте версию Node.js (должна быть 20.x)

### ❌ Serverless Function timeout
**Проблема:** API запросы занимают слишком много времени

**Решение:**
1. Оптимизируйте код API
2. Используйте кеширование
3. Рассмотрите Vercel Pro для увеличения лимитов

---

## 💰 Стоимость

### Vercel Free Tier включает:
- ✅ Unlimited deployments
- ✅ 100 GB bandwidth/месяц
- ✅ 100 GB-hours Serverless Function execution
- ✅ Автоматический SSL
- ✅ Preview deployments

### Когда нужен Pro ($20/месяц):
- Больше 100 GB трафика
- Больше 100 GB-hours функций
- Расширенная аналитика
- Приоритетная поддержка

---

## 🔐 Безопасность

### Переменные окружения:
- ✅ Никогда не коммитьте `.env` файлы
- ✅ Используйте Vercel Environment Variables
- ✅ Разделяйте переменные для Production/Preview/Development

### API ключи:
- ✅ Храните в Vercel Environment Variables
- ✅ Используйте разные ключи для разных окружений
- ✅ Регулярно ротируйте ключи

---

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel Serverless Functions](https://vercel.com/docs/functions)
- [Vite Documentation](https://vitejs.dev/)
- [Express.js Guide](https://expressjs.com/)

---

## 🎯 Следующие шаги

1. ✅ Задеплойте проект на Vercel
2. ✅ Настройте кастомный домен (опционально)
3. ✅ Настройте GitHub интеграцию для автоматических деплоев
4. ✅ Добавьте мониторинг и алерты
5. ✅ Настройте Preview deployments для Pull Requests

---

**Готово! Ваш проект полностью работает на Vercel! 🎉**
