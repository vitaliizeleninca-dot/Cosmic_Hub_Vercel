# 🚂 Развертывание Backend на Railway

## Быстрый старт

### 1️⃣ Подготовка
```bash
git add .
git commit -m "Deploy to Railway"
git push
```

### 2️⃣ Создание проекта
1. Откройте [railway.app](https://railway.app)
2. Войдите через GitHub
3. **New Project** → **Deploy from GitHub repo**
4. Выберите ваш репозиторий

### 3️⃣ Переменные окружения

Добавьте в Railway (Settings → Variables):

```env
NODE_ENV=production
PORT=${{PORT}}
FRONTEND_URL=https://ваш-frontend.vercel.app
```

Если используете GitHub API:
```env
GITHUB_TOKEN=ваш_токен
```

### 4️⃣ Получение URL

1. Settings → Networking
2. **Generate Domain** - получите бесплатный домен
3. Ваш backend будет доступен: `https://ваш-проект.railway.app`

### 5️⃣ Проверка

Откройте в браузере:
- Health check: `https://ваш-проект.railway.app/health`
- API test: `https://ваш-проект.railway.app/api/ping`

## 🔗 Подключение Frontend

Обновите API URL в вашем frontend коде:

```typescript
// client/lib/api-config.ts
const API_URL = import.meta.env.PROD 
  ? 'https://ваш-проект.railway.app'
  : 'http://localhost:3001';

export default API_URL;
```

## 📊 Что происходит при деплое

Railway автоматически:
1. ✅ Клонирует репозиторий
2. ✅ Устанавливает зависимости (`pnpm install`)
3. ✅ Собирает проект (`pnpm run build:all`)
4. ✅ Запускает сервер (`node dist/server/dev.js`)

## 🔄 Автоматические обновления

Каждый `git push` автоматически запускает новый деплой!

## 💰 Стоимость

- **$5** бесплатных кредитов каждый месяц
- Примерно **$5-10/месяц** для небольших проектов
- Pay-as-you-go модель

## 🐛 Решение проблем

### Ошибка сборки
```bash
# Проверьте локально
pnpm install
pnpm run build:all
```

### Ошибка запуска
- Проверьте логи в Railway (Deployments → View Logs)
- Убедитесь, что файл `dist/server/dev.js` создается

### CORS ошибки
Добавьте URL frontend в `FRONTEND_URL` переменную окружения

## 📁 Созданные файлы

- ✅ `railway.json` - конфигурация Railway
- ✅ `nixpacks.toml` - настройки сборки
- ✅ `Procfile` - команда запуска
- ✅ `RAILWAY_DEPLOY.md` - подробная документация (EN)

## 🎯 Следующие шаги

1. Задеплойте backend на Railway
2. Получите URL вашего backend
3. Обновите API URL в frontend
4. Задеплойте frontend на Vercel/Netlify
5. Добавьте URL frontend в `FRONTEND_URL` на Railway

## 📞 Полезные ссылки

- [Railway Docs](https://docs.railway.app/)
- [Railway Discord](https://discord.gg/railway)
- [Подробная инструкция](./RAILWAY_DEPLOY.md)
