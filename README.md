# 🚀 Fusion Starter - Full-Stack Vercel Deployment

Полнофункциональный стартер с React + Vite frontend и Express backend, готовый к развертыванию на Vercel.

## ✨ Особенности

- ⚡ **Vite** - быстрая сборка и HMR
- ⚛️ **React 18** - современный UI
- 🎨 **Tailwind CSS** - utility-first стилизация
- 🧩 **ShadCN UI** - готовые компоненты
- 🔧 **Express API** - backend на Vercel Serverless Functions
- 📝 **Decap CMS** - управление контентом
- 🎯 **TypeScript** - типобезопасность
- 🚀 **Vercel Ready** - готов к деплою

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
pnpm install

# Запуск dev сервера (frontend + backend)
pnpm dev
```

Откройте [http://localhost:8080](http://localhost:8080)

### Развертывание на Vercel

**Полная инструкция:** [VERCEL_ONLY_RU.md](./VERCEL_ONLY_RU.md) 🇷🇺

**Быстрый деплой:**

1. Закоммитьте код:
```bash
git add .
git commit -m "Deploy to Vercel"
git push
```

2. Откройте [vercel.com](https://vercel.com)
3. Импортируйте репозиторий
4. Нажмите **Deploy**

**Готово!** Frontend и Backend работают на одном домене! 🎉

## 📁 Структура проекта

```
├── api/
│   └── index.ts              # Vercel Serverless Function (все API)
├── server/
│   ├── index.ts              # Express app
│   └── routes/               # API маршруты
├── client/                   # React frontend
│   ├── components/           # UI компоненты
│   ├── pages/                # Страницы
│   └── lib/                  # Утилиты
├── public/                   # Статические файлы
├── vercel.json               # Конфигурация Vercel
└── package.json
```

## 🔌 API Endpoints

После деплоя доступны:

- `GET /health` - проверка работоспособности
- `GET /api/ping` - тестовый endpoint
- `GET /api/demo` - демо данные
- `GET /api/nft-collection` - NFT коллекция
- `GET /api/youtube-duration` - длительность YouTube видео
- `POST /api/send-message` - отправка сообщений
- `GET /api/links` - получение ссылок
- `POST /api/save-link` - сохранение ссылки

## 🛠️ Доступные команды

```bash
# Разработка
pnpm dev              # Frontend + Backend (localhost:8080)
pnpm dev:server       # Только Backend (localhost:3001)

# Сборка
pnpm build            # Сборка frontend
pnpm build:all        # Сборка frontend + backend

# Другое
pnpm typecheck        # Проверка типов
pnpm format.fix       # Форматирование кода
pnpm test             # Запуск тестов
```

## 🌍 Переменные окружения

Создайте `.env` файл (локально) или добавьте в Vercel Settings:

```env
# Builder.io (опционально)
VITE_PUBLIC_BUILDER_KEY=your_key

# GitHub API (опционально)
GITHUB_TOKEN=your_token

# Другие
NODE_ENV=production
PING_MESSAGE=Hello!
```

**Важно:** Переменные с префиксом `VITE_` доступны в frontend.

## 📚 Документация

- 🇷🇺 [Быстрая инструкция по Vercel](./VERCEL_ONLY_RU.md)
- 🇬🇧 [Подробная документация Vercel](./VERCEL_DEPLOY.md)
- 🚂 [Развертывание на Railway](./RAILWAY_DEPLOY_RU.md)

## 🎨 UI Компоненты

Проект использует [ShadCN UI](https://ui.shadcn.com/) - коллекцию переиспользуемых компонентов:

- Buttons, Cards, Dialogs
- Forms, Inputs, Selects
- Modals, Tooltips, Dropdowns
- И многое другое...

Все компоненты в `client/components/ui/`

## 🔐 Безопасность

- ✅ `.env` файлы в `.gitignore`
- ✅ Переменные окружения через Vercel
- ✅ CORS настроен правильно
- ✅ TypeScript для типобезопасности

## 💰 Стоимость

**Vercel Free Tier включает:**
- Безлимитные деплои
- 100 GB трафика/месяц
- 100 GB-hours Serverless функций
- Автоматический SSL
- Preview deployments

**Для большинства проектов бесплатного плана достаточно!**

## 🤝 Вклад

Contributions welcome! Открывайте issues и pull requests.

## 📄 Лицензия

MIT

---

**Готово к деплою на Vercel! 🚀**

Нужна помощь? Откройте [VERCEL_ONLY_RU.md](./VERCEL_ONLY_RU.md)
